# Optional: enables grounded LLM explanations through OpenRouter.
OPENROUTER_API_KEY=replace_with_openrouter_api_key

# Comma-separated explicit frontend origins. Do not use * with credentials.
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
